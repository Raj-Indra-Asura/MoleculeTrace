# Exercise 03 — Repository map (Required)

**Goal:** never again wonder where a file belongs.

## Part A — where does it go?

For each artefact, write the directory it belongs in and one sentence of
justification.

| Artefact | Directory | Why |
|----------|-----------|-----|
| A `CREATE TABLE molecule ...` migration | | |
| A saved analytical query you will re-run | | |
| The Streamlit dashboard entry point | | |
| A pytest suite for the FastAPI service | | |
| A pytest suite validating week 09's constraints | | |
| Synthetic activity data generated by a script | | |
| An architecture decision record | | |
| A screenshot proving week 17's index worked | | |
| A short note pointing to the week that teaches hashing | | |
| A worked solution to a week's exercise | | |

## Part B — the rules in your words

1. What is the `labs/` rule, and what happens when content is duplicated between
   `labs/` and `curriculum/week-XX/`?

2. Why must starter files never contain a finished solution?

3. Where do credentials live, and which file must never be committed?

## Part C — the learning contract

Answer from `ROADMAP.md` and README section 5.5:

4. Week 12 depends on which weeks, and how would you notice if you had skipped
   one?

5. What makes a week "complete"?

6. What must be true of every week's project outcome?

**Required output:** the completed table and answers 1–6.
