# Shape of the psql checks in exercise 02. Patch versions and paths vary.

moleculetrace=# SELECT version();
                                                     version
------------------------------------------------------------------------------------------------------------------
 PostgreSQL 16.4 (Debian 16.4-1.pgdg120+1) on x86_64-pc-linux-gnu, compiled by gcc (Debian 12.2.0-14) 12.2.0, 64-bit
(1 row)

moleculetrace=# \conninfo
You are connected to database "moleculetrace" as user "moleculetrace" via socket in "/var/run/postgresql" at port "5432".

moleculetrace=# SELECT count(*) AS user_tables
moleculetrace-# FROM information_schema.tables
moleculetrace-# WHERE table_schema NOT IN ('pg_catalog', 'information_schema');
 user_tables
-------------
           0
(1 row)

moleculetrace=# SELECT current_schema();
 current_schema
----------------
 public
(1 row)
