# expected-outputs/

Reference outputs used to check your work: query result snippets, `EXPLAIN`
plan shapes, API response bodies, metric tables.

These are targets, not solutions — they show *what* the result should look like,
not *how* to produce it. Row ordering is irrelevant unless a file says otherwise.
Numeric values produced from synthetic data are labelled synthetic and may vary
within the tolerance each file states.
